# AI API Keys
# IMPORTANT: Replace this with your actual GROQ API key in the .env file
# Never commit real API keys to version control!
GROQ_API_KEY=replace-with-your-actual-groq-api-key

# Database
# Replace with your actual database URL in the .env file
DATABASE_URL=replace-with-your-actual-database-url

# Session Secret
# Generate a new random secret in your .env file
# You can use a command like: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=replace-with-a-secure-random-string

# Node Environment
NODE_ENV=production
