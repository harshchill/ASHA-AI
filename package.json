{
  "name": "chatbot-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Chatbot backend scaffold for Render.com deployment.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "start:backend": "npm install --prefix server && npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.6.3",
    "dotenv": "^16.3.1",
    "compromise": "^13.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
